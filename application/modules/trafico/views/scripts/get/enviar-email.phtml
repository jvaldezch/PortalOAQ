<p style="font-size: 12px; padding: 10px 0">¿Desea enviar un correo electrónico con pedimentos adjuntos a los siguientes contactos?</p>

<?php if (isset($this->contacts)) { ?>
    <table class="traffic-table traffic-table-left">
        <thead>
            <tr>
                <th>
                    <input type="checkbox" id="all-emails" checked="true" />
                </th>
                <th style="text-align: left; font-size: 11px">Email</th>
                <th style="text-align: left; font-size: 11px">Nombre receptor</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($this->contacts as $k => $contact) { ?>
                <tr>
                    <td style="text-align: center"><input type="checkbox" checked="true" class="customer-email" /></td>
                    <td style="text-align: left; font-size: 11px"><?= $contact["email"] ?></td>
                    <td style="text-align: left; font-size: 11px"><?= $contact["nombre"] ?></td>
                </tr>
                <script>emailData["emails"][<?= $k ?>] = <?= $contact["id"] ?></script>
            <?php } ?>
        </tbody>
    </table>
<?php } else { ?>
    <p style="font-size: 11px; margin: 5px 0; color: #ff3333">No hay contactos para este cliente.</p>
<?php } ?>

<p style="font-size: 12px; padding: 10px 0">Con los siguiente archivos adjuntos:</p>

<?php if (isset($this->archivos)) { ?>
    <table class="traffic-table traffic-table-left">
        <thead>
            <tr>
                <th>&nbsp;</th>
                <th style="text-align: left; font-size: 11px">Archivo</th>
                <th style="text-align: left; font-size: 11px">Tipo Archivo</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($this->archivos as $k => $file) { ?>
                <tr>
                    <td style="text-align: center"><input type="checkbox" checked="true" /></td>
                    <td style="text-align: left; font-size: 11px"><img src="/images/icons/attachment.gif" /><?= $file["nom_archivo"] ?></td>        
                    <td style="text-align: left; font-size: 11px"><?= $file["nombre"] ?></td>        
                </tr>
                <script>emailData["archivos"][<?= $k ?>] = <?= $file["id"] ?></script>
            <?php } ?>
        </tbody>
    </table>
<?php } else { ?>
    <p style="font-size: 11px; margin: 5px 0; color: #ff3333">No hay archivos (pedimentos) para enviar.</p>
<?php } ?>

<script src="/js/trafico/post/enviar-email.js"></script>
    